# 专利状态检查调度指南

## 一句话目标

日常使用时，我们只关心一个问题：

```text
这个专利现在要不要检查一下状态？
```

本指南和 `update_by_strategy.py` 的目的，就是把这个问题自动算出来。脚本会根据每个申请号的最新状态和上次检查时间，生成“现在应该检查”的申请号清单。

我们的业务目标是尽早发现申请是否进入：

```text
驳回等复审请求
```

所以，本策略主要服务于“驳回前状态检查”。如果某个申请已经进入 `驳回等复审请求`，说明它已经命中我们要发现的目标状态，不再由这套“驳回前检查策略”继续轮询。

## 判断口径

对每个申请号，脚本只做一个判断：

```text
现在是否需要检查状态？
```

判断步骤如下：

1. 读取该申请号在 `data/results/detection_log.json` 中的最新状态。
2. 查看这个状态是否在 `data/focus_strategy.json` 的检查规则里。
3. 如果不在规则里，说明当前不需要由本策略检查。
4. 如果在规则里，读取该申请号的上次检查时间 `timestamp`。
5. 用当前时间减去上次检查时间。
6. 如果间隔已经达到该状态对应的检查周期，就加入本次检查清单。
7. 如果间隔还没到，就暂时跳过。

可以理解成：

```text
状态需要跟踪 且 距离上次检查已经足够久
= 现在要检查
```

## 内部检查规则

下面这张表是脚本内部使用的判断规则。日常使用者不需要手工按这些组拆分，只需要运行 `generate` 生成当前应检查清单。

| 最新状态 | 当前数量 | 内部检查间隔 | 判断原因 |
|----------|---------:|-------------:|----------|
| `中通回案实审` | 155 | 7 天 | 已到较后审查阶段，可能较快出现驳回或其他结论 |
| `中通出案待答复` | 185 | 14 天 | 已发中间审查意见，状态变化风险较高 |
| `一通出案待答复` | 203 | 30 天 | 第一轮审查意见答复阶段，按月检查即可 |
| `一通回案实审` | 301 | 30 天 | 第一轮答复后继续实审，按月检查即可 |
| `进入审查` | 74 | 45 天 | 刚进入实审，通常变化较慢 |
| **合计** | **918** | - | 当前驳回前状态检查池 |

这些规则来自：

```text
data/focus_strategy.json
```

如果以后要调整某个状态多久检查一次，只改这个配置文件即可。

## 不由本策略继续检查的情况

以下情况不会进入本策略生成的检查清单：

| 最新状态类型 | 示例 | 处理方式 |
|--------------|------|----------|
| 已经命中目标状态 | `驳回等复审请求` | 不再进入驳回前检查清单 |
| 已失效或撤回 | `驳回失效`、`逾期视撤失效`、`撤回专利申请` | 不继续检查 |
| 已授权或授权后状态 | `专利权维持`、`等年登印费`、`准备颁证公告` | 不属于本策略 |
| 复审阶段状态 | `复审程序中` 等 | 如需跟踪，应另建复审后策略 |

当前日志中已有 `驳回等复审请求` 1382 件。它们是我们已经识别出来的目标对象，不是这套驳回前状态检查清单的继续更新对象。

## 单个专利的判断示例

### 示例 1：现在要检查

```text
申请号：A
最新状态：中通回案实审
上次检查：8 天前
内部间隔：7 天
结论：现在要检查
```

原因：这个状态仍属于驳回前跟踪范围，而且已经超过 7 天。

### 示例 2：现在不用检查

```text
申请号：B
最新状态：中通出案待答复
上次检查：5 天前
内部间隔：14 天
结论：现在不用检查
```

原因：虽然状态需要跟踪，但距离上次检查还太近。

### 示例 3：不进入本策略

```text
申请号：C
最新状态：驳回等复审请求
结论：不进入驳回前检查清单
```

原因：它已经到达我们要发现的目标状态。

## 日常使用流程

### 1. 判断单个专利是否要检查

如果只想看某一个申请号现在要不要检查：

```bash
python update_by_strategy.py check <申请号>
```

示例：

```bash
python update_by_strategy.py check 202211273995X
```

脚本会直接输出结论：

```text
结论: 现在要检查状态
```

或：

```text
结论: 现在暂时不用检查状态
```

### 2. 生成当前要检查的清单

推荐使用：

```bash
python update_by_strategy.py generate
```

它会生成当前所有“现在要检查”的申请号。

如果没有到期对象，脚本会提示当前没有需要更新的申请号。这时不要继续运行旧清单。

### 3. 启动 MITM 代理

在终端 1：

```bash
source venv/bin/activate
python start_mitm_proxy.py
```

### 4. 执行本次检查

在终端 2：

```bash
source venv/bin/activate
USE_MITM_PROXY=true python main_automation.py --update-list data/update_list_dynamic.txt
```

如果你运行的是带周期参数的命令，例如：

```bash
python update_by_strategy.py generate 7
```

就使用脚本输出的对应文件，例如：

```bash
USE_MITM_PROXY=true python main_automation.py --update-list data/update_list_dynamic_7days.txt
```

日常建议优先使用不带周期参数的 `generate`，因为它直接回答“现在有哪些专利要检查”。

## 查看判断结果

如果想看脚本为什么这么判断，可以运行：

```bash
python update_by_strategy.py status
```

它会按内部检查间隔显示：

- 当前共有多少申请号在该规则下；
- 其中多少现在要检查；
- 其中多少暂时不用检查；
- 最早应该检查的是哪些申请号。

当前基线下，2026-05-09 的结果是：

| 内部间隔 | 总数 | 现在要检查 |
|----------|-----:|-----------:|
| 7 天 | 155 | 155 |
| 14 天 | 185 | 0 |
| 30 天 | 504 | 0 |
| 45 天 | 74 | 0 |

这张表只是解释脚本的判断结果，不是要求人工分别管理这些组。

## 检查后会发生什么

执行 `main_automation.py --update-list ...` 后，程序会重新采集这些申请号，并更新 `data/results/detection_log.json` 中的：

- 最新状态；
- 专利基础字段；
- 发文信息；
- `timestamp`。

下一次运行 `update_by_strategy.py generate` 时，脚本会基于新的状态和新的 `timestamp` 重新判断。

结果可能是：

| 检查后的最新状态 | 下一次处理 |
|------------------|------------|
| 仍然是驳回前状态 | 按对应间隔继续等待下一次检查 |
| 变成 `驳回等复审请求` | 从驳回前检查清单退出 |
| 变成授权、失效、撤回等状态 | 从本策略退出 |
| 变成其他未配置状态 | 暂不进入本策略，后续按需要补规则 |

## 关键文件

| 文件 | 作用 | 日常是否手动维护 |
|------|------|------------------|
| `update_by_strategy.py` | 判断现在哪些专利要检查 | 否 |
| `data/focus_strategy.json` | 内部状态检查规则 | 偶尔维护 |
| `data/results/detection_log.json` | 当前专利数据和上次检查时间 | 否 |
| `data/update_list_dynamic.txt` | 当前所有要检查的申请号 | 脚本生成 |
| `data/update_list_dynamic_7days.txt` 等 | 某个内部间隔下要检查的申请号 | 脚本生成 |
| `data/update_list_7days.txt` 等旧文件 | 早期静态快照 | 不建议日常运行 |
| `data/update_list_reject_retry.txt` | 已进入驳回等复审状态的历史快照 | 不属于本策略主流程 |

日常入口应以 `update_by_strategy.py generate` 的输出为准。

## 规则维护

只有当我们想改变“某种最新状态多久检查一次”时，才需要维护 `data/focus_strategy.json`。

常见维护场景：

- 某个状态比预期更容易快速进入 `驳回等复审请求`，需要缩短检查间隔；
- 某个状态长期变化很慢，可以拉长检查间隔；
- CNIPA 出现新的状态名称，需要加入规则；
- 业务决定把某个新状态纳入驳回前检查。

修改后运行：

```bash
python update_by_strategy.py stats
python update_by_strategy.py status
```

确认规则和数量符合预期。

## 第二阶段优化说明

### 什么是"状态变化检测"

当前的流程是：
```
generate → 生成"现在要检查"的清单 → 执行采集 → 更新 detection_log
```

**问题**：更新后，我们需要知道"状态是否发生了变化"。

例如：
- 这个申请在检查前是 `中通回案实审`
- 检查后变成了 `驳回等复审请求`
- **这就是我们要发现的目标状态！** 🎯

### 实现内容

**1. 保存"前一次状态"**
- 在 detection_log.json 中扩展字段，记录 `previous_status`
- 这样就能在检查后对比状态变化

**2. 生成"状态变化日志"**
- 新命令：`python update_by_strategy.py diff` 
- 显示检查前后的状态差异
- 分类显示：进入驳回、授权、失效等关键变化

**3. 自动分流"已命中目标的申请"**
- 自动识别进入 `驳回等复审请求` 的申请
- 从驳回前检查清单中移除
- 可选：另行生成"已命中"清单供后续处理

### 生成详细统计报告

包含维度：
- 按申请年份：2023年、2024年的申请各有多少在驳回前检查范围
- 按状态分布：各个驳回前状态的申请数量和比例
- 按时间范围：多少申请已经"超期"（距离上次检查超过规定周期）
- 按申请人TOP10：哪些申请人的专利最需要关注

### 何时实施

- 🔄 **当前**（第一阶段完成）：可选继续优化
- 📋 **建议**：先执行一次检查更新，看看实际数据反馈，再决定是否实施第二阶段

## 暂不放入当前主流程的内容

以下内容可以以后扩展，但不是当前“这个专利现在要不要检查状态”的主流程：

| 扩展内容 | 当前处理 |
|----------|----------|
| 对 `驳回等复审请求` 做复审后持续跟踪 | 另建复审后策略 |
| 根据答复期限自动调整检查间隔 | 需要可靠期限字段后再实现 |
| 状态变化告警 | 可基于检查前后状态差异另做 |
| 月度 KPI 报告 | 可后续统计，不影响当前判断 |

## 当前实现结论

当前实现已经能回答核心问题：

```text
这个专利现在要不要检查一下状态？
```

实现方式是：

- 用最新状态决定它是否属于驳回前检查范围；
- 用上次检查时间和内部间隔决定现在是否到期；
- 只把现在到期的申请号写入动态清单；
- 检查后再用最新状态重新判断下一次是否还需要检查。

## 修改历史

### v2.3 - 2026-05-09 【第二阶段优化】

状态变化检测与详细报告功能（完成）：

- ✅ **初始化 previous_status 字段**：在 detection_log.json 中添加 `previous_status` 字段，用于记录检查前的状态
- ✅ **新增 `diff` 命令**：`python update_by_strategy.py diff` - 显示申请号的状态变化分析
  - 自动分类识别：进入驳回、进入授权、失效/撤回、进入复审、其他变化
  - 重点标记进入 `驳回等复审请求` 的申请（关键目标）
  - 演示结果：可正确识别所有状态变化类型
- ✅ **新增 `report` 命令**：`python update_by_strategy.py report` - 详细的多维度统计报告
  - 申请年份分布：2020-2026各年份的申请数统计
  - 驳回前各状态分布：五个监控状态的具体计数和比例
  - 检查周期超期统计：按周期分组统计超期数量
  - 申请人 TOP10：风险最高的申请企业排名
- 📊 **当前统计数据**：
  - 驳回前监控范围：918 件申请
  - 2022年申请最多：305 件（占比34.2%）
  - 风险申请人 TOP1：国网浙江省电力有限公司湖州供电公司（29件）
  - 当前超期情况：0件（生成后未执行采集）

**工作流程优化**：
1. 运行 `generate` 生成本次检查清单 → 
2. 执行采集更新 detection_log.json → 
3. 运行 `diff` 识别状态变化 → 
4. 运行 `report` 查看全面统计

### v2.2 - 2026-05-09 【第一阶段优化】

代码质量与数据验证改进（完成）：

- ✅ **修复 Bug**：修复 `show_update_status()` 函数的参数比较逻辑错误
- ✅ **代码重构**：提取 `sort_by_update_time()` 函数，消除列表排序逻辑重复
- ✅ **新增验证命令**：`python update_by_strategy.py validate` - 验证 focus_strategy.json 计数与 detection_log.json 一致性
  - 当前验证结果：所有 918 件申请号的状态计数均与配置一致 ✓
- 📊 **当前状态**：
  - 7天周期需要检查：155 件（中通回案实审，已超期5天）
  - 14-45天周期：暂无需检查，下个触发点为 05-11

### v2.1 - 2026-05-09

调整文档表达：

- 把主问题收敛为”这个专利现在要不要检查状态”；
- 将 7 / 14 / 30 / 45 天分层说明调整为内部检查规则；
- 日常入口改为优先使用 `python update_by_strategy.py generate`；
- 弱化人工按周期组管理的表述。

### v2.0 - 2026-05-09

重写策略指南，使文档与当前计划保持一致：

- 明确主目标是发现进入 `驳回等复审请求` 的申请；
- 明确本策略只覆盖驳回前状态；
- 移除主流程中的 3 天复审后监测描述；
- 明确动态清单和静态清单的区别；
- 补充日常执行流程和策略维护规则。

### v1.0 - 2026-05-08

创建按频率分类的专利审查程序监测策略。
